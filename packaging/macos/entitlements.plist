<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow the app to be run as a hardened runtime app -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Allow network access for update checking -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow file access for reading/writing study data -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow access to Downloads folder for exports -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Allow executable memory (required for some Rust code) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for some dependencies) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation for third-party libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow dyld environment variables (useful for debugging) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
