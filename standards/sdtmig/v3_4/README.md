# standards/sdtmig/v3_4

Offline SDTMIG v3.4 metadata.

- `Datasets.csv` — dataset/domain metadata (class, label, structure)
- `Variables.csv` — variable metadata per domain (includes Core/Req expectations)
- `SDTMIG_v3.4.pdf` — optional (if present) for reference only
